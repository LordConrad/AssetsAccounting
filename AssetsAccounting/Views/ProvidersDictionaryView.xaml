<UserControl x:Class="AssetsAccounting.Views.ProvidersDictionaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Label Style="{StaticResource LabelStyle}" Content="{Binding HeaderText}"></Label>
        <Label Content="{Binding ProviderContent}"></Label>
        <StackPanel Grid.Row="2">
            <Button Command="{Binding AddProviderCommand}">Добавить</Button>
        </StackPanel>
    </Grid>
</UserControl>
